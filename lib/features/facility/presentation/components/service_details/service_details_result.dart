/// Result popped by [FacilityServiceDetailsScreen] when the user taps the
/// "View Pricing" CTA, so the caller can switch the facility screen's main
/// tab to Pricing.
enum FacilityServiceDetailsResult { viewPricing }
